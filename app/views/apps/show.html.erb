<div class="container">
  <h1 class="title-all"><%=@app.name%></h1>
    <p><%=@app.URL%></p>
    <h2 class="title-all"><%= pluralize(@app.events.count, 'event')%></h2>
    <div class="event-index">
    <% @app.events.each do |event| %>
        <p><%= link_to event.name, app_event_path(@app, event) %> <small>  submitted <%= time_ago_in_words(event.created_at) %> ago </small></p>
    <% end %>
       <%= link_to "Delete Application", @app, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this application' } %>
       <%= link_to "New Event", new_app_event_path(@app), class: 'btn btn-success' %>
       <%= pie_chart @app.events.group(:name).count %>
    </div>
</div>
